import React from 'react';

const NavButton = () => {
    return (
        <div>
            <button className='border border-[#e0e0e0] px-[24px] py-3 rounded-[12px] font-fustat'>Exit</button>
        </div>
    );
};

export default NavButton;